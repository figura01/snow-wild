export enum UserRoleEnum {
  admin = 'ADMIN',
  user = 'USER',
}

export enum StatutReservation {
  AWAITING = 'en_attente',
  CONFIRMATION = 'confirmée',
  PAID = 'payée',
  CANCEL = 'annulée',
  FINISHED = 'terminée',
}
